diff --git a/node_modules/@once-ui-system/core/dist/components/Mask.js b/node_modules/@once-ui-system/core/dist/components/Mask.js
index e7c090e..c9c0f2b 100644
--- a/node_modules/@once-ui-system/core/dist/components/Mask.js
+++ b/node_modules/@once-ui-system/core/dist/components/Mask.js
@@ -34,19 +34,31 @@ const Mask = forwardRef(({ cursor = false, x, y, radius = 50, reducedMotion = "a
     useEffect(() => {
         if (!isActive)
             return;
-        const handleMouseMove = (event) => {
+        let lastClientX = 0;
+        let lastClientY = 0;
+        const updateFromClient = (clientX, clientY) => {
             const el = maskRef.current;
-            if (!el)
-                return;
-            const rect = el.getBoundingClientRect();
-            cursorPosRef.current = {
-                x: event.clientX - rect.left,
-                y: event.clientY - rect.top,
-            };
+            if (el) {
+                const rect = el.getBoundingClientRect();
+                cursorPosRef.current = {
+                    x: clientX - rect.left,
+                    y: clientY - rect.top,
+                };
+            }
+        };
+        const handleMouseMove = (event) => {
+            lastClientX = event.clientX;
+            lastClientY = event.clientY;
+            updateFromClient(event.clientX, event.clientY);
+        };
+        const handleScroll = () => {
+            updateFromClient(lastClientX, lastClientY);
         };
         document.addEventListener("mousemove", handleMouseMove, { passive: true });
+        window.addEventListener("scroll", handleScroll, { passive: true });
         return () => {
             document.removeEventListener("mousemove", handleMouseMove);
+            window.removeEventListener("scroll", handleScroll);
         };
     }, [isActive]);
     useEffect(() => {
